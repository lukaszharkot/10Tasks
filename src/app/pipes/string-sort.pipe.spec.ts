import { StringsSortPipe } from './strings-sort.pipe';

describe('StringsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new StringsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
